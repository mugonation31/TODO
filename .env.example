# =============================================================================
# TODO Application - Environment Configuration Template
# =============================================================================
# Copy this file to backend/.env and fill in your actual values
# DO NOT commit the actual .env file with secrets to git
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# For local Supabase: http://localhost:54321
# For hosted Supabase: https://your-project.supabase.co
SUPABASE_URL=http://localhost:54321

# JWT Secret from Supabase (Dashboard → Settings → API → JWT Settings)
# For local Supabase, use: super-secret-jwt-token-with-at-least-32-characters-long
SUPABASE_JWT_SECRET=super-secret-jwt-token-with-at-least-32-characters-long

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# Local Supabase: postgresql://postgres:postgres@localhost:54322/postgres
# Hosted Supabase: postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Allowed CORS origins (comma-separated, no spaces)
CORS_ORIGINS=http://localhost:4200,http://localhost:4200/

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------
# Environment: development, staging, or production
ENVIRONMENT=development
